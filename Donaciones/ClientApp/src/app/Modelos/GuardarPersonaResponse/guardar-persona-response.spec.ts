import { GuardarPersonaResponse } from './guardar-persona-response';

describe('GuardarPersonaResponse', () => {
  it('should create an instance', () => {
    expect(new GuardarPersonaResponse()).toBeTruthy();
  });
});
